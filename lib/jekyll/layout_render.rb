module Jekyll

  class LayoutRender < Plugin
    # Initialize the LayoutRender.
    #
    # Returns an initialized LayoutRender.
    def initialize(config = {})
      @config = config
    end
  end

end
